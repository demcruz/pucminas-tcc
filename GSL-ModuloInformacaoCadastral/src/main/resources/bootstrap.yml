server:
  port: 8082  
  name: /informacaoCadastral
spring:
  application:
    name: informacaoCadastral
  cloud:
    config:
      name: application-mysqlServer, eureka-default
      enabled: true
      uri: http://localhost:8888      
security:
  jwt:
    token:
      secret-key: chave_microservices
      expire-length: 36000000  
      
